import React from 'react';
import './style.css';

const Header = () => {
    return (
        <header className="header">
            <h1><span>Geocoding</span> <span>service</span></h1>
        </header>
    );
};

export default Header;
